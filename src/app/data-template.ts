export interface WINES {
  id?: number;
  imageUrl: string;
  isNew?: boolean;
  label: string;
  isFinished?: boolean;
  progress?: number;
}

export interface HERO {
  title: string;
  subtitle: string;
  imageUrl: string;
  description: string;
}
